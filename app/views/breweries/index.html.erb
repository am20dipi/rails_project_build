<h1>Breweries</h1>

<% @breweries.each do |brewery| %>
    <ul>
        <li><%= link_to brewery.name, "/breweries/#{brewery.id}" %></li>
    </ul>
<% end %>

<br>
<br>
<%= link_to "Add a New Brewery", new_brewery_path %>